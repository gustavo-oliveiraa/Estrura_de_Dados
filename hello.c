#include<stdio.h>

int main(){

    // var
    // I/O - scanf / printf
    // alocação de espaços em memória...
    // nome, tipo, valor *tamanho
    // nome (indentificador): sem espaço, char especiais, 
    // tipo primitivos - int, char, float(6), double, (longint)
    // valores... 5 '5' "5" 5.0

    // operadores
    // aritméticos: + - * / %
    // == = i++(salto) ++i i-=3

    // Estruturas

    // Condicionais
    // if, if/else, switch...

    // Repetição
    // for, while, do..while

    // Estrutura de dados compostas homogênea
    // -> vetores, strings, matrizes
        
    // Estrutura de dados compostas heterogêneas
    // -> struct (struct de struct?, vetor de struct)
    // typedef (definição de tipo)

    // ponteiros

    // manipulação de arquivos?

    // modularização
    // funções e procedimentos

    printf("Hello, World!");

    return 0;
}